# Optional dependencies. Required ONLY for the components named below.
# Everything in the primary pipeline (Stage 0/1/3/4, baselines, metrics,
# statistics, adversarial edits) runs without them.

# --- Stage 0 graph encoder on large graphs (D2/D4/D5) ---
torch-geometric>=2.5.2       # manuscript S4.8.2: PyTorch Geometric 2.5.2

# --- Stage 2 schema-constrained rationale language model ---
transformers>=4.40.1         # manuscript S4.8.2: Transformers 4.40.1
peft>=0.10.0                 # manuscript S4.8.2: PEFT 0.10.0
accelerate>=0.29
# Grammar-masked decoding backend. The manuscript specifies grammar-masked
# decoding but NOT the library used -> see docs/KNOWN_LIMITATIONS.md (L-07).
# The repository ships its own incremental JSON grammar masker with no
# third-party dependency; this entry is only for an alternative backend.
# llama-cpp-python>=0.2.70

# --- Stage 2 training-only retrieval index ---
sentence-transformers>=2.7.0 # manuscript S4.3.4: all-MiniLM-L6-v2
faiss-cpu>=1.8.0             # manuscript S4.8.2 used FAISS-GPU 1.8.0
